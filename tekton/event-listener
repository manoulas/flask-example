apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: event-listener
spec:
  serviceAccountName: tekton-robot
  triggers:
    - name: event-trigger
      bindings:
      - ref: trigger-binding
      template:
        ref: trigger-template
